import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'vue_first_app',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
